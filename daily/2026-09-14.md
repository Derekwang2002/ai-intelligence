# AI Intelligence — 2026-09-14

> 本次运行扫描窗口为 [2026-09-13T01:02:19Z, 2026-09-13T16:00:31Z]，检索 overlap 回退至 2026-09-12T22:02:19Z。新增 1 个事件，更新 2 个既有事件，跳过 1 个重复发布。事件按官方 UTC 日期归档；本日报按 Asia/Shanghai 自然日归档。

## Daily Executive Summary

- **LiteLLM RC 加入 Skills 发现与 MCP 权限（ev-20260913-01，WATCH）**：1.102.0-rc.1 通过 well-known index 发布 Agent Skills，并可为 Codex 与 Claude Code 配置 gateway key。按用户的 MCP 工具权限同时覆盖 `tools/list` 与 `tools/call`，另有客户 KMS、路由遥测和护栏失效路径修复。方向完整，但仍是 RC，生产升级应等 stable。
- **DeepSeek V4.1-Flash 的 vLLM 支持进入合并代码（ev-20260910-01 更新）**：DeepSelect 在 GB200、batch 256 / 1M KV 的单 kernel 测试中为 191 微秒，原路径为 616 微秒。Engram 表也加入 node-local DP 分片、异步 CPU offload 预取与共享内存。两项都只在 vLLM main，不能当成正式版本的端到端性能承诺。
- **Qwen3.8-Flash-Next-NVFP4 有了单机上游部署路径（ev-20260826-04 更新）**：SGLang main 已支持单台 DGX Spark / GB10，并公布 200 题 GSM8K 与吞吐检查。该结果只有 TP=1、不是受控前后对照，维持 WATCH。

## Models

- **DeepSeek V4.1-Flash（TRIAL）**：vLLM #56464 与 #56512 分别补齐稀疏索引 TopK、Engram DP 分片与 offload 路径。框架集成在变实，但尚未进入 tagged release。
- **Qwen3.8-Flash-Next（WATCH）**：SGLang #39126 把 NVIDIA NVFP4 衍生版带到单台 DGX Spark；这是可复现的工作站路径，不是完整生产验证。

## Agent & AI Engineering

- **LiteLLM 1.102.0-rc.1（WATCH）**：Skills 发现、MCP 授权、gateway key、客户自管 KMS 与路由可观测性收进同一控制面。POC 应重点测 `tools/list` 与 `tools/call` 权限一致性、费用统计和护栏 fail-closed 行为。
- Gemini CLI 0.61 nightly 于 01:29Z 再发同源码 hash。官方 diff 只有 package 版本与 sandbox image tag，按重复项跳过。

## Open Source

- 本窗口的高价值变化集中在框架 main 分支：vLLM 为 DeepSeek V4.1-Flash 合并两个 serving 路径，SGLang 为 Qwen Flash-Next 的 NVFP4 衍生版合并单机路径。
- Codex 只有未发布的 main commits；llama.cpp 只有滚动构建与局部修复，均未立事件。

## Research

- arXiv 水位线仍为 2609.11923，严格窗口内没有新的 v1/v2 时间戳；周日 digest 尚未出现。

## Developer Tools

- Codex stable 仍为 0.154.0，Claude Code 仍为 2.1.270，Gemini CLI stable 仍为 0.59.0。LiteLLM RC 可自动配置 Codex/Claude Code gateway，但不改变这些客户端版本线。

## Infrastructure

- DeepSelect 的 191 微秒结果只覆盖 GB200 上的 TopK kernel。Engram 预取的 TTFT 改善最高仅 0.9%。选型时不要把两者外推为端到端吞吐提升。
- Qwen NVFP4 路径在 GB10 上验证 200 题 GSM8K：带/不带 MTP 分别为 97.5%/97.0%，吞吐 92.87/89.53 tok/s；样本和并发设置不同，不能直接比较 MTP 收益。

## Business & Policy

- 本窗口没有足以改变模型获取、API 成本、芯片供给或开源格局的新政策事件。Anthropic sitemap 在扫描截止后出现刷新，留待下次按实际内容核查。

## Trend Signals

本周期没有发现证据充分的新趋势。**趋势 #1** 新增一组 serving 生态证据：DeepSeek 与 Qwen 两条开放权重模型线分别得到 vLLM 与 SGLang 的合并代码，但第三方 Terminal-Bench / SWE 权重复现仍缺，维持 strengthening / Medium。**趋势 #3** 的技能/编排标准化观察项出现 LiteLLM RC 邻接信号，因未进入 stable 不计正式证据。其余趋势状态与置信度不变。

## Tech Radar

- 新 **LiteLLM 1.102.0-rc.1**（ai-engineering / WATCH）：Agent Skills 发现与 MCP 授权进入统一 gateway；等 stable 再评估生产升级。
- **DeepSeek V4.1-Flash**（foundation-model / TRIAL）：vLLM 合并 DeepSelect 与 Engram 生产路径，但尚未进入正式版本。
- **Qwen3.8-Flash-Next**（foundation-model / WATCH）：SGLang 合并单 DGX Spark NVFP4 路径，验证范围仍窄。

当前雷达：ADOPT 6 / TRIAL 59 / WATCH 79，共 144 条追踪条目。完整当前雷达以站点「洞察」页为准。

## Worth Trying

- 在隔离环境安装 LiteLLM RC，验证 MCP 工具列表与实际调用的用户权限是否严格一致，并做一次升级前后的费用对账。
- 若在 GB200 部署 DeepSeek V4.1-Flash，单独复测 DeepSelect kernel，再跑端到端 TTFT/吞吐，避免把 PR 微基准直接带入容量规划。
- 若有 DGX Spark，用 SGLang #39126 的参数复现 Qwen NVFP4 路径，并扩大到完整 GSM8K 与 coding agent 任务。

## Watch Items

- LiteLLM 1.102 stable 是否保留本次 Agent Skills、MCP 权限与 KMS 行为，并公布升级说明。
- vLLM 下一 tagged release 是否包含 #56464/#56512；SGLang 下一 tagged release 是否包含 #39126。
- 趋势 #1 的最后判据：GLM-5.3 或 DeepSeek V4.1-Flash 公开权重的第三方 Terminal-Bench / SWE 复现。
- 下一个 arXiv digest 从 2609.11923 之后继续扫描；Anthropic 截止后 sitemap 变动按实际发布时间复核。

## Sources

- https://github.com/BerriAI/litellm/releases/tag/v1.102.0-rc.1 、https://pypi.org/project/litellm/1.102.0rc1/
- https://github.com/vllm-project/vllm/pull/56464 、https://github.com/vllm-project/vllm/pull/56512
- https://github.com/sgl-project/sglang/pull/39126
- https://github.com/google-gemini/gemini-cli/releases/tag/v0.61.0-nightly.20260913.g9c1b0a610 、https://github.com/google-gemini/gemini-cli/compare/v0.61.0-nightly.20260912.g9c1b0a610...v0.61.0-nightly.20260913.g9c1b0a610
- https://openai.com/news/rss.xml 、https://github.blog/changelog/feed/ 、https://blog.google/technology/ai/rss/ 、https://arxiv.org/list/cs.AI/recent
